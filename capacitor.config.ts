import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bscpe3c.setapp',
  appName: 'setapp',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
